# commandes en linux :

cd 
ls 
ls -a 
touch
rm -r
sudo apt-get update
sudo apt install python3.10-venv
# pour créer un environnement : 
python3 -m venv venv
# pour activer et ecrire dans la venv : 
source venv/bin/activate
# pour installer les librairies des requirements
nano requirements.txt
# ou 
pip install -r requirements.txt


# pour updater le projet en local :
git pull && git fetch
# pour creer une branche : 
git checkout -b

# se mettre sur une branche : 
git checkout Apparence_based_testing

# ligne pratique pour trouver path fichier
ROOT_DIR = os.getcwd()
print(": wasa",ROOT_DIR)
%pip install -r $ROOT_DIR/requirements.txt